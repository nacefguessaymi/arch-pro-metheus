## Setup note: isyncrc

`isyncrc.template` is committed with placeholder addresses. The real
`~/.config/isyncrc` is gitignored since it contains actual email addresses.
On a fresh install:
```bash
cp dotfiles/.config/isyncrc.template ~/.config/isyncrc
# then edit in the real addresses
```
